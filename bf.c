#!/usr/local/bin/tcc -run -b -bt80 -DUSE_UNSIGNED_INT -DUSE_COMPUTED_GOTO
/*
	A very quick, convenient way to run the assembler with built-in emulator.
	This version is for debugging the assembler
	
	Truly we are blessed 

	(Without Ncurses)
*/
#include "assembler.c"
