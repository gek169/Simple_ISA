#!/usr/local/bin/tcc -run -lSDL2 -lSDL2_mixer -DUSE_UNSIGNED_INT -DUSE_SDL2 -DUSE_COMPUTED_GOTO
/*
	A very quick, convenient way to run the assembler with built-in emulator.
	Truly we are blessed 
*/
#include "assembler.c"
